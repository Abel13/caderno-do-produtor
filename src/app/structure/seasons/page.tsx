import Link from"next/link";import{redirect}from"next/navigation";import{ArrowRight}from"@/components/icons";import{createClient}from"@/lib/supabase/server";import{chooseActiveProperty}from"@/modules/identity/domain/permissions";import{requireIdentityContext}from"@/modules/identity/infrastructure/supabase/server-context";import{RuralStructureRepository}from"@/modules/rural-structure/infrastructure/supabase/rural-structure-repository";import{StructureForms}from"@/modules/rural-structure/presentation/components/structure-forms";
export default async function SeasonsPage(){const{context}=await requireIdentityContext();const property=chooseActiveProperty(context.properties,context.profile.last_property_id);if(!property)redirect("/structure");const data=await new RuralStructureRepository(await createClient()).overview(property.id);const role=context.memberships.find(m=>m.account_id===property.account_id)?.role;const canWrite=role==="owner"||role==="manager";const seasonStatusLabel=(status:string)=>status==="open"?"Aberta para registros" : status==="closed" ? "Encerrada (somente consulta)" : "Em planejamento";const seasonStatusDescription=(status:string)=>status==="open" ? "Ciclo produtivo ativo: use para abrir e registrar operações." : status==="closed" ? "Ciclo encerrado: mantido para histórico e análise." : "Preparação inicial da safra, sem efeito em produção ainda.";return <main className="min-h-screen bg-[#f7f6f1] px-4 py-8"><div className="mx-auto max-w-4xl"><Link href="/structure" className="inline-flex min-h-11 items-center gap-2 text-emerald-800"><ArrowRight className="size-4 rotate-180"/>Visão geral</Link><h1 className="mt-5 text-3xl font-bold">Safras de {property.name}</h1><p className="mt-2 text-stone-600">Consulte períodos já cadastrados e a situação de cada ciclo.</p><section className="mt-6 rounded-2xl border bg-white p-5">{data.seasons.length?<div className="divide-y">{data.seasons.map(s=><article key={s.id} className="py-4"><div className="flex justify-between gap-3"><div><h2 className="font-bold">{s.name}</h2><p className="text-sm text-stone-500">{s.starts_on} até {s.ends_on}</p></div><div className="text-right"><span className="text-sm font-semibold text-emerald-800">{seasonStatusLabel(s.status)}</span><p className="text-xs text-stone-600">{seasonStatusDescription(s.status)}</p></div></div></article>)}</div>:<p className="text-sm text-stone-600">Nenhuma safra cadastrada.</p>}</section>{canWrite?<StructureForms propertyId={property.id} plots={data.plots} cultivars={data.cultivars} hasSeason={data.seasons.length>0} initialStep="season"/>:<p className="mt-6 rounded-xl bg-amber-50 p-4 text-amber-900">Acesso para consulta. Você não pode cadastrar ou alterar safras.</p>}</div></main>}
