import Link from "next/link";import{redirect}from"next/navigation";import{ArrowRight,Calendar,Check,Coffee}from"@/components/icons";import{createClient}from"@/lib/supabase/server";import{chooseActiveProperty}from"@/modules/identity/domain/permissions";import{requireIdentityContext}from"@/modules/identity/infrastructure/supabase/server-context";import{structureChecklist}from"@/modules/rural-structure/domain/progress";import{RuralStructureRepository}from"@/modules/rural-structure/infrastructure/supabase/rural-structure-repository";
export default async function StructurePage(){const{context}=await requireIdentityContext();const property=chooseActiveProperty(context.properties,context.profile.last_property_id);if(!property)redirect("/onboarding");const data=await new RuralStructureRepository(await createClient()).overview(property.id);const plantings=data.plots.flatMap(plot=>plot.plantings);const checklist=structureChecklist({plots:data.plots.length,plantings:plantings.length,seasons:data.seasons.length,links:data.linkCount});const membership=context.memberships.find(item=>item.account_id===property.account_id);const canWrite=membership?.role==="owner"||membership?.role==="manager";return <main className="min-h-screen bg-[#f7f6f1] px-4 py-8"><div className="mx-auto max-w-5xl"><Link href="/dashboard" className="inline-flex min-h-11 items-center gap-2 text-emerald-800"><ArrowRight className="size-4 rotate-180"/>Dashboard</Link><p className="mt-6 text-sm font-semibold text-emerald-800">ESTRUTURA DA PROPRIEDADE</p><h1 className="mt-1 text-3xl font-bold">{property.name}</h1><p className="mt-2 text-stone-600">Consulte o que já existe ou continue pelo próximo cadastro necessário.</p>{!canWrite&&<p className="mt-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-900"><strong>Acesso para consulta.</strong> Somente proprietário e gestor podem alterar a estrutura.</p>}<section className="mt-6 rounded-2xl border bg-white p-5"><h2 className="font-bold">Progresso da configuração</h2><ol className="mt-4 grid gap-3 sm:grid-cols-5">{checklist.map((item,index)=><li key={item.key} className="flex gap-2 sm:block"><span className={item.done?"grid size-7 place-items-center rounded-full bg-emerald-700 text-white":"grid size-7 place-items-center rounded-full bg-stone-100"}>{item.done?<Check className="size-4"/>:index+1}</span><p className="text-sm font-semibold sm:mt-2">{item.label}</p></li>)}</ol></section><section className="mt-6 grid gap-4 sm:grid-cols-3"><Area href="/structure/plots" title="Talhões" value={`${data.plots.length} cadastrados`} text="Áreas físicas da propriedade"/><Area href="/structure/plantings" title="Lavouras" value={`${plantings.length} cadastradas`} text="Café plantado em cada talhão"/><Area href="/structure/seasons" title="Safras" value={`${data.seasons.length} cadastradas`} text="Períodos e ciclos produtivos"/></section><section className="mt-8 rounded-2xl border bg-white p-5"><div className="flex items-center gap-2"><Calendar className="size-5 text-emerald-700"/><h2 className="text-xl font-bold">Safras recentes</h2></div>{data.seasons.length?<div className="mt-4 divide-y">{data.seasons.map(season=><div key={season.id} className="flex items-center justify-between gap-3 py-3"><div><p className="font-semibold">{season.name}</p><p className="text-sm text-stone-500">{new Intl.DateTimeFormat("pt-BR").format(new Date(`${season.starts_on}T12:00:00`))} a {new Intl.DateTimeFormat("pt-BR").format(new Date(`${season.ends_on}T12:00:00`))}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-800">{season.status==="open"?"Aberta":season.status==="closed"?"Encerrada":"Planejamento"}</span></div>)}</div>:<p className="mt-4 text-sm text-stone-600">Nenhuma safra cadastrada. Acesse Safras para criar a primeira.</p>}</section></div></main>}
function Area({href,title,value,text}:{href:string;title:string;value:string;text:string}){return <a href={href} className="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm transition hover:border-emerald-600"><Coffee className="size-5 text-emerald-700"/><h2 className="mt-3 text-lg font-bold">{title}</h2><p className="mt-1 text-sm font-semibold text-emerald-800">{value}</p><p className="mt-2 text-sm text-stone-500">{text}</p><span className="mt-4 inline-block text-sm font-bold text-emerald-800">Ver e gerenciar →</span></a>}
