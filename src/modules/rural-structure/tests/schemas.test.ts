import{describe,expect,it}from"vitest";import{plotSchema,plantingSchema,seasonSchema}from"../domain/schemas";
describe("rural structure schemas",()=>{it("accepts decimal comma and optional cultivar",()=>{expect(plotSchema.parse({propertyId:crypto.randomUUID(),name:"Talhão 1",areaHa:"2,5",status:"active"}).areaHa).toBe(2.5);expect(plantingSchema.parse({plotId:crypto.randomUUID(),cultivarId:"",areaHa:"2",plantedYear:"",status:"forming"}).cultivarId).toBeNull()});it("rejects an inverted season",()=>{expect(seasonSchema.safeParse({propertyId:crypto.randomUUID(),name:"2026/27",startsOn:"2027-01-01",endsOn:"2026-01-01",status:"open"}).success).toBe(false)})});
