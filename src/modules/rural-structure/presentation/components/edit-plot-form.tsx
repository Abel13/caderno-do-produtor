"use client";
import{useActionState,useState}from"react";import{Button}from"@/components/ui/button";import type{PlotSummary}from"../../infrastructure/supabase/rural-structure-repository";import{updatePlotAction,type StructureActionState}from"../actions";import{PlotFields}from"./structure-forms";
const initial:StructureActionState={status:"idle"};
export function EditPlotForm({plot,propertyId}:{plot:PlotSummary;propertyId:string}){const[open,setOpen]=useState(false);const[state,action,pending]=useActionState(async(previous:StructureActionState,formData:FormData)=>{const next=await updatePlotAction(previous,formData);if(next.status==="success")setOpen(false);return next},initial);const values=state.values??{name:plot.name,areaHa:String(plot.area_ha),status:plot.status};if(!open)return <Button variant="ghost" size="sm" onClick={()=>setOpen(true)}>Editar</Button>;return <div className="fixed inset-0 z-50 grid place-items-center overflow-y-auto bg-stone-950/40 p-4" role="dialog" aria-modal="true" aria-labelledby={`edit-plot-${plot.id}`}><form action={action} className="w-full max-w-xl space-y-4 rounded-2xl bg-white p-5 shadow-xl sm:p-7"><div><h3 id={`edit-plot-${plot.id}`} className="text-lg font-bold">Editar talhão</h3><p className="mt-1 text-sm text-stone-600">Use os mesmos campos do cadastro. A alteração ficará registrada no histórico.</p></div><input type="hidden" name="plotId" value={plot.id}/><input type="hidden" name="propertyId" value={propertyId}/><PlotFields values={values} includeLifecycle/>{state.message&&state.status==="error"&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-800">{state.message}</p>}<div className="flex flex-col gap-2 sm:flex-row"><Button disabled={pending}>{pending?"Salvando...":"Salvar alterações"}</Button><Button type="button" variant="ghost" onClick={()=>setOpen(false)}>Cancelar</Button></div></form></div>}
