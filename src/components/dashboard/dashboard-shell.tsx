import { Bell, Calendar, CloudRain, Coffee, FileText, Home, Plus, Settings, TrendingUp, Users } from "@/components/icons";
import { Button } from "@/components/ui/button";
import { PropertySelector } from "@/components/molecules/property-selector";
import { accountRoleLabel } from "@/modules/identity/domain/permissions";
import type { IdentityContext, IdentityProperty } from "@/modules/identity/domain/types";

export function DashboardShell({ context, activeProperty }: { context: IdentityContext; activeProperty: IdentityProperty }) {
  const firstName = context.profile.full_name?.split(" ")[0] ?? "produtor";
  const membership = context.memberships.find((item) => item.account_id === activeProperty.account_id);
  const canManage = membership && ["owner", "manager"].includes(membership.role);
  const roleLabel = membership ? accountRoleLabel(membership.role) : "Sem vínculo";
  return <div className="min-h-screen bg-[#f7f6f1] text-stone-900"><header className="sticky top-0 z-20 border-b border-stone-200/80 bg-[#f7f6f1]/95 backdrop-blur"><div className="mx-auto flex min-h-16 max-w-7xl items-center gap-3 px-4 py-2 sm:px-6"><div className="flex items-center gap-2 font-bold"><span className="grid size-9 place-items-center rounded-xl bg-emerald-800 text-white"><Coffee className="size-5" aria-hidden="true"/></span><span className="hidden sm:inline">Caderno do Produtor</span></div><div className="ml-auto flex items-center gap-2"><div className="hidden text-right md:block"><p className="text-xs text-stone-500">{membership?.account_name}</p><p className="text-sm font-bold text-emerald-800">{roleLabel}</p></div><PropertySelector properties={context.properties} activeId={activeProperty.id}/><button aria-label="Notificações" className="relative grid size-11 place-items-center rounded-xl border border-stone-200 bg-white"><Bell className="size-5" aria-hidden="true"/>{context.profile.internal_notifications_enabled && <span className="absolute right-2 top-2 size-2 rounded-full bg-amber-500"/>}</button></div></div></header><div className="mx-auto flex max-w-7xl"><aside className="sticky top-16 hidden h-[calc(100vh-4rem)] w-64 shrink-0 border-r border-stone-200 p-4 lg:block"><div className="mb-4 rounded-xl bg-white p-3"><p className="text-xs text-stone-500">Seu acesso</p><p className="mt-1 font-bold text-emerald-800">{roleLabel}</p></div><nav className="space-y-1 text-sm font-medium"><Nav href="/dashboard" Icon={Home}>Visão geral</Nav><Nav href="#" Icon={Calendar}>Atividades</Nav><Nav href="#" Icon={CloudRain}>Clima e água</Nav><Nav href="#" Icon={FileText}>Documentos</Nav>{canManage && <Nav href="/settings/access" Icon={Users}>Acessos</Nav>}<Nav href="/settings/profile" Icon={Settings}>Configurações</Nav></nav></aside><main className="min-w-0 flex-1 px-4 py-7 sm:px-6 lg:py-10"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><p className="text-sm text-stone-500">{activeProperty.city} · {activeProperty.state}</p><span className="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-900 md:hidden">{roleLabel}</span></div><h1 className="mt-1 text-3xl font-bold tracking-tight">Olá, {firstName}</h1><p className="mt-2 text-stone-600">Acompanhe a produção e o manejo de {activeProperty.name}.</p></div><Button size="lg"><Plus className="size-5" aria-hidden="true"/>Novo registro</Button></div><section className="mt-8 rounded-3xl bg-emerald-900 p-6 text-white shadow-sm sm:p-8"><p className="text-sm font-semibold text-emerald-200">PRODUÇÃO DA SAFRA</p><h2 className="mt-2 text-3xl font-bold">Ainda sem registros de produção</h2><p className="mt-2 max-w-xl text-emerald-100">Quando a safra e os talhões forem cadastrados, estimativas e resultados aparecerão aqui.</p><a href="/structure" className="mt-6 inline-flex min-h-11 items-center rounded-xl bg-white px-5 text-sm font-semibold text-stone-800">Preparar estrutura da lavoura</a></section><section className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card Icon={CloudRain} label="Chuva no mês" value="Sem dados"/><Card Icon={Calendar} label="Próximas atividades" value="Nenhuma"/><Card Icon={Coffee} label="Talhões ativos" value="0"/><Card Icon={FileText} label="Documentos" value="Nenhum pendente"/></section><section className="mt-8 rounded-2xl border border-stone-200 bg-white p-6 text-center shadow-sm"><TrendingUp className="mx-auto size-8 text-emerald-700" aria-hidden="true"/><h2 className="mt-3 text-lg font-bold">Seu histórico começa aqui</h2><p className="mx-auto mt-2 max-w-md text-sm text-stone-500">Cadastre talhões e uma safra para transformar os registros do campo em indicadores úteis.</p></section></main></div></div>;
}
function Nav({ href, Icon, children }: { href:string; Icon:typeof Home; children:React.ReactNode }) { return <a href={href} className="flex min-h-11 items-center gap-3 rounded-xl px-3 py-3 text-stone-600 transition hover:bg-white hover:text-emerald-900"><Icon className="size-5" aria-hidden="true"/>{children}</a>; }
function Card({ Icon, label, value }: { Icon:typeof Home; label:string; value:string }) { return <article className="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><span className="grid size-10 place-items-center rounded-xl bg-emerald-50 text-emerald-800"><Icon className="size-5" aria-hidden="true"/></span><p className="mt-4 text-sm text-stone-500">{label}</p><p className="mt-1 text-xl font-bold">{value}</p></article>; }
